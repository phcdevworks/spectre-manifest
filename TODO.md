# Spectre Manifest Execution Todo

No active work is currently open in this package. Phases 1 and 2 are
complete — see [ROADMAP.md](ROADMAP.md) for the full delivery history and
[CHANGELOG.md](CHANGELOG.md) for release-by-release detail.

New contract work here remains demand-driven: it opens only when a
downstream repo (`spectre-shell`, `spectre-shell-router`,
`spectre-shell-signals`, `spectre-init`, or a repo in another project area)
surfaces a concrete missing schema field, validation rule, or tooling gap.

When a new phase opens, add it here with the same P0/P1 structure the
completed phases used in `ROADMAP.md`.

## Explicitly Out of Scope

- Do not define UI behavior or component logic here
- Do not absorb token generation or CSS output
- Do not add package-specific runtime code

## Requested by Downstream

None yet.
